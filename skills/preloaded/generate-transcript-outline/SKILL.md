---
name: generate-transcript-outline
description: 从已审计的文字稿时间轴和结构化知识生成前台可读的内容大纲，为每章生成时间范围、核心内容、关键知识点和可定位时间戳。适用于生成文字稿章节导航、修复只有段落 ID 而无可读章节内容的大纲时使用。
---

# 生成文字稿内容大纲

大纲是前台导航层，不是新的知识提取层。正式输出使用 JSON Schema v1；Wiki 页面只保存经后端校验后的内容产物，前端通过标准 JSON 字段渲染。

## 输入

后端提供当前转写代次、完整时间轴、分块清单和分块正文。该 Skill 不调用 WeKnora Agent 工具。

### 时间戳处理

- 章节时间范围和原文时间戳来源于 **精读源文档** 返回的原始文字稿内容，而非 Wiki 页面。
- 原始文字稿有时间戳时，大纲中每段原文显示 `> **说话人** HH:MM:SS–HH:MM:SS` 格式。
- 原始文字稿无时间戳时，章节时间范围标注 `unknown`，原文段落不显示时间戳，不伪造。

生成前完整阅读 [references/outline-template.md](references/outline-template.md)。

## 工作流程

### 一、建立章节骨架

1. 读取当前转写代次的完整分块，确认章节顺序、边界和时间戳。
2. 保留经审计的陈述性章节标题；如上游标题是反问、设问或其他疑问形式，先回到时间轴阶段修正，不在大纲层静默改名。

### 二、逐章生成内容

对每个章节依次执行步骤 3～7，章节之间相互独立：

3. 使用转写证据和可选知识原子组织本章，不重复提取新结论。
4. 生成每章 1～2 个全片关键知识点；只有存在独立结论、方法或动作时才新增点位。输出字段和约束见 [references/outline-template.md](references/outline-template.md)。
5. 按说话人和段落顺序展示本章原文。
6. 有真实原文定位链接时提供跳转；否则只显示时间范围。
7. 显示该章的对齐状态和必要警示。

### 三、输出

8. 所有章节内容就绪后，由后端校验 JSON Schema v1，再保存为 WeKnora 内容页。写入契约：`page_type` 用 `index`；页面 frontmatter 必须含 `type: outline`、`source_video_id: {视频ID}`、`transcript_generation: {转写代次}` 与 `schema_version: 1`。由 vidsage 内容流水线触发时，页面 slug 固定为 `outline/{视频ID}`，不得使用视频标题或其他产物 slug。

## 强制规则

- 不得只输出 `P001–P008` 而不展示对应原文。
- 正式章节输出必须是 JSON Schema v1；不得返回 `...`、空对象或只有 Markdown 的非结构化正文。
- 时间字段只使用数字秒数；时间显示文本由后端或前端格式化，不由模型拼接。
- 原文完整保存在 Wiki 产物中供追溯；章节导航前端展示章节内容和时间戳，不展示原文。
- 不得改变原文的主体、语气、范围、因果和确定程度。
- 知识点服务于全片复习，不为覆盖每句原文而切分；同义观点、例子和论据合并到同一个知识点。
- 每章默认 1～2 个知识点，必要时最多 3 个；全片优先控制在 8～16 个，不得为了凑数拆分。
- 知识点标题使用短语或结论式短标题，采用“方法名 / 动作 + 对象 / 核心结论”结构，控制在 4～10 个字或 2～5 个英文词，不写完整句。
- 章节核心内容必须能回指章节 ID 和知识原子 ID。
- 不展示"核心问题"或"本章回答"；章节标题与核心内容已经承担导航和解释作用。
- 将章节的 `topic_tags` 统一展示为"关键词"，不要使用"主题"或"关键主题"标签。
