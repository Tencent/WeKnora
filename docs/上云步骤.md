# WeKnora (vidsage) 上云部署指南

> **版本**: v0.7.2 | **目标服务器**: 腾讯云 CVM 2核4G | **部署方式**: Docker Compose
> **编写日期**: 2026-08-18 | **分支**: custom/main
> **代码仓库**: `https://github.com/lizi586586-code/vidsage.git`
> **参考**: 附录 A — WeKnora 标准版上云步骤

---

## 目录

1. [云资源评估](#一云资源评估)
2. [部署前准备](#二部署前准备)
3. [初始化云服务器](#三初始化云服务器)
4. [离线中转固定镜像](#四离线中转固定镜像)
5. [获取代码并确定版本](#五获取代码并确定版本)
6. [配置生产环境变量](#六配置生产环境变量)
7. [为 Redis 增加持久化卷](#七为-redis-增加持久化卷)
8. [启动 5 个核心服务](#八启动-5-个核心服务)
9. [通过公网 IP 验证](#九通过公网-ip-验证)
10. [配置域名与 HTTPS](#十配置域名与-https)
11. [上线验收](#十一上线验收)
12. [备份与恢复](#十二备份与恢复)
13. [升级与回滚](#十三升级与回滚)
14. [安全红线](#十四安全红线)

---

## 服务清单

| 类型 | Compose 服务 | 默认对外端口 | 是否应暴露公网 |
|------|-------------|-------------|--------------|
| Web 前端与反向代理 | `frontend` | `80` | 是，仅暴露 80/443 |
| Go 主服务 | `app` | `8080` | 否，仅供本机诊断和前端内网调用 |
| Python 文档解析服务 | `docreader` | `50051` | 否 |
| 主数据库与默认向量检索 | `postgres`（ParadeDB/PostgreSQL） | `5432` | 否 |
| 队列与流状态 | `redis` | `6379` | 否 |

默认调用关系：

```
公网用户
-> frontend（Vue 静态页面 + NGINX）
-> app（REST、SSE、RAG、Agent、异步 Worker）
-> postgres（业务数据、BM25、pgvector）
-> redis（Asynq、流状态、Pub/Sub、限流）
-> docreader（gRPC 文档解析）
-> 外部 LLM / Embedding / Rerank API
```

标准部署不需要单独部署 Qdrant、Milvus、Neo4j、MinIO、Langfuse 或 MCP Server。需要相关能力时再按 profile 增加，不建议首次部署直接使用 `--profile full`。

> 本文档不包含 GPU 和本地大模型部署。资源评估按调用外部模型 API 计算；如果计划在同一台机器运行 Ollama，需要单独评估 CPU、内存或 GPU。

---

## 一、云资源评估

### 1. 业务规模假设

当前已知规模：
- 使用人数约 10 人；
- 预计同时活跃用户约 2～4 人；
- 文档约 20 份；
- 文档原始体积合计约 100MB；
- 使用 PostgreSQL 作为默认关系数据库、BM25 与向量检索引擎；
- 使用本地 Docker Volume 保存上传文件；
- LLM、Embedding、Rerank 使用外部 API，不在本机运行模型；
- 暂不启用 Neo4j、MinIO、Langfuse、独立向量库及 `full` profile；
- 同一时间尽量只上传、解析一份大文档。

> "100MB 大小"存在两种口径，本文默认按 **20 份文档合计约 100MB** 估算。如果是 **每份文档约 100MB**，总原始文件约 2GB，应采用下文的增强配置，并注意单文件上传限制。

### 2. 推荐购买规格

| 资源 | 第一版本规格 | 后续升级规格 | 说明 |
|------|-----------|-----------|------|
| CPU | 2 vCPU | 4 vCPU | 10 人规模的日常 API 并发较低；2 核解析速度会更慢，但不影响基本功能 |
| 内存 | 4GB + 2-4GB Swap | 8GB | 4GB 可以运行 5 个核心服务，但必须限制解析和 Worker 并发 |
| 系统/数据盘 | 60GB SSD | 100GB SSD | 同时容纳镜像、PostgreSQL、上传文件、解析图片、日志和至少一份备份 |
| 公网带宽 | 5Mbps + 500GB/月 | 10Mbps | 5Mbps 上传 100MB 理论约 160 秒；经常上传大文件再升到 10Mbps |
| 公网 IP | 1 个 | 1 个固定公网 IP | 用于用户访问和域名解析 |
| GPU | 不需要 | 不需要 | 前提是使用外部模型 API |

### 3. 当前服务器配置（已购买）

| 项目 | 规格 |
|------|------|
| 云厂商 | 腾讯云 |
| 实例类型 | CVM |
| CPU | 2 核 |
| 内存 | 4 GB |
| 系统盘 | SSD 云硬盘 60 GB |
| 流量包 | 500 GB/月（带宽 5 Mbps） |
| 到期时间 | 2027-11-12 22:51:39 |
| 域名 | 备案中 |

> 当前配置与第一版本规格完全匹配，60GB SSD 比推荐的 50GB 起步略宽裕。

### 4. 资源消耗拆分

以下数值用于理解容量来源，不是当前 Compose 已配置的硬性限制：

| 服务 | 典型资源特征 |
|------|-----------|
| `frontend` | 本地空闲实测约 15MB；按低配云环境预留 64～128MB |
| `app` | 本地空闲实测约 321MB；任务运行时按约 500MB～1GB 观察 |
| `docreader` | 本地空闲实测约 267MB；解析时可能升至 1～2GB 以上，是主要峰值来源 |
| `postgres` | 本地当前数据量下约 85MB；初期按约 256～512MB 观察 |
| `redis` | 本地空闲实测约 20MB；当前队列规模按 128～256MB 观察 |
| 操作系统与 Docker | 预留约 700MB～1GB；Swap 只用于吸收短时峰值，不能替代内存 |

> 本地实测中，Docker 可用内存约 3.83GiB，5 个核心容器空闲时合计约 707MB，且没有 OOM 或自动重启，因此 4GB 并非无法运行。但本地 Docker 有 8 个可用 CPU，而云主机只有 2 vCPU；云端的主要差异会表现为文档解析更慢、并发任务更容易排队。

### 5. 100MB 文档与默认上传限制

项目默认单文件上限为 50MB：
```dotenv
MAX_FILE_SIZE_MB=50
DOCREADER_GRPC_MAX_FILE_SIZE_MB=50
```

- 如果 100MB 是 20 份文档的合计大小，并且每份均小于 50MB，不需要修改。
- 如果存在单份 100MB 文件，建议把两项都设置为 `120`，给协议开销留出余量：
```dotenv
MAX_FILE_SIZE_MB=120
DOCREADER_GRPC_MAX_FILE_SIZE_MB=120
```

如果服务器前面还有云负载均衡或宿主机 NGINX，也必须同步把请求体上限提高到至少 120MB，否则请求会在到达 WeKnora 前被拒绝。

### 6. 扩容观察指标

以下情况触发升配到 4 vCPU / 8GB：
- 单份文件接近 100MB；
- 经常同时上传、解析多份 PDF、PPT 或扫描件；
- 会启用 OpenDataLoader、OCR、知识图谱或大量 Wiki 生成任务；
- 计划在同机部署 MinIO、Neo4j、Langfuse 等可选组件；
- 解析期间内存持续超过 85%，或发生 OOM/容器重启；
- Swap 持续大量使用、Redis 队列长期积压。

上线后重点观察：
- 主机内存持续超过 80%；
- 磁盘使用率超过 70%；
- 文档解析期间出现 OOM 或容器重启；
- Redis 队列持续积压；
- PostgreSQL 查询延迟明显升高；
- 外部模型 API 的限流和超时。

当前规模优先采用纵向升级（4GB 升到 8GB），暂时不需要把 5 个服务拆到多台机器。

---

## 二、部署前准备

### 1. 推荐部署形态

本文采用：
- 一台 Ubuntu Server 22.04 LTS 或 24.04 LTS 云主机；
- Docker Engine + Docker Compose v2；
- WeKnora 标准版 `docker-compose.yml`；
- PostgreSQL、Redis、上传文件都在本机持久化；
- 外部模型 API；
- 公网 IP 用于首次验证，域名 + HTTPS 用于正式使用。

### 2. 安全组规则

在腾讯云控制台为 CVM 实例配置安全组：

| 方向 | 协议/端口 | 来源 | 用途 |
|------|----------|------|------|
| 入站 | TCP 22 | 仅管理员固定 IP | SSH 运维 |
| 入站 | TCP 80 | `0.0.0.0/0`、`::/0` | HTTP 与证书签发 |
| 入站 | TCP 443 | `0.0.0.0/0`、`::/0` | HTTPS 正式访问 |
| 出站 | TCP 443 | 允许 | 拉取镜像、调用模型和第三方 API |
| 出站 | DNS/NTP | 按云厂商默认 | 域名解析与时间同步 |

**不要向公网开放**：
- `8080`：app
- `50051`：docreader
- `5432`：PostgreSQL
- `6379`：Redis

> Docker 发布的端口可能绕过部分主机防火墙规则，因此公网边界应优先依赖云安全组；`app` 端口同时绑定到 `127.0.0.1`。

### 3. SSH 连接与排障

轻量应用服务器的 Ubuntu 镜像可能使用 `admin` 作为 Workbench 与 SSH 用户，不要直接假设用户名一定是 `root`。先通过腾讯云控制台的 Workbench 一键连接确认实际用户名，再把本机公钥加入该用户的 `~/.ssh/authorized_keys`。

如果 SSH 报 `Operation timed out`，故障发生在公钥或密码认证之前，配置公钥本身不会解决超时。可按以下顺序检查：

```bash
sudo systemctl is-active ssh
sudo ss -lntp 'sport = :22'
sudo ufw status verbose
```

预期 SSH 为 `active`、监听 `*:22`，且没有宿主机防火墙阻断。

如果服务器完全收不到 22 端口数据包，可在明确授权的短暂诊断窗口内临时把 22 放开，发起一次 SSH 连接，然后立即从服务器日志确认真实来源 IP：

```bash
sudo journalctl -u ssh --since '5 minutes ago' --no-pager \
  | grep '<实际SSH用户名>' \
  | tail -n 5
```

连接出现 `Permission denied (publickey)` 说明网络已经打通，只剩公钥认证问题。取得日志中的真实 SSH 来源 IP 后，必须立即把云防火墙的 TCP 22 恢复为该单个地址的 `/32`，不得长期保留 `0.0.0.0/0`。随后通过 Workbench 将本机 `.pub` 公钥追加到实际用户的 `~/.ssh/authorized_keys`，并设置目录权限为 `700`、文件权限为 `600`；确认公钥 SSH 登录成功后再继续部署。

### 4. 准备域名

正式环境建议准备一个域名，例如：
```text
kb.example.com
```

在 DNS 控制台添加 A 记录，指向云服务器的固定公网 IP。DNS 生效前可以先使用 `http://公网IP` 验证。

---

## 三、初始化云服务器

以下命令以 Ubuntu 为例。

### 1. 更新系统与安装基础工具

```bash
sudo apt update
sudo apt upgrade -y
sudo apt install -y ca-certificates curl git make openssl
```

### 2. 为 4GB 内存配置 Swap

第一版本建议配置 2-4GB Swap，用于吸收文档解析的短时内存峰值。先检查云镜像是否已有 Swap：

```bash
sudo swapon --show
```

如果没有输出，创建 Swap 文件：

```bash
if [ ! -f /swapfile ]; then
  sudo fallocate -l 4G /swapfile
  sudo chmod 600 /swapfile
  sudo mkswap /swapfile
fi
sudo swapon /swapfile
grep -qF '/swapfile none swap sw 0 0' /etc/fstab \
  || echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

验证：

```bash
free -h
sudo swapon --show
```

> Swap 只能避免短时 OOM，不能解决持续内存不足。如果解析期间长期使用超过 1GB Swap，应升级到 8GB 内存。

### 3. 安装 Docker Engine 与 Compose v2

生产环境建议使用 Docker 官方 apt 仓库，不使用便捷安装脚本：

```bash
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc
sudo tee /etc/apt/sources.list.d/docker.sources >/dev/null <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo systemctl enable --now docker
```

验证：

```bash
sudo docker version
sudo docker compose version
sudo systemctl is-active docker
sudo docker network create weknora-install-check
sudo docker network rm weknora-install-check
```

> 官方安装说明：https://docs.docker.com/engine/install/ubuntu/
>
> `docker` 用户组等同于高权限。本文保留 `sudo docker`，不强制把普通用户加入该组。

---

## 四、离线中转固定镜像

如果 Docker 已正常安装，但访问 `registry-1.docker.io:443` 超时，或者个人镜像加速器对目标标签返回 `MANIFEST_UNKNOWN`，不要改用 `latest`，也不要在 2 vCPU / 4GB 服务器上构建源码。可以在能够通过 VPN 访问 Docker Hub 的 Mac 上拉取固定的 `linux/amd64` 官方镜像，再通过 SSH 传到服务器。

> WeKnora 0.7.2 在 Docker Hub 上的正式标签带 `v`，即 `v0.7.2`；`0.7.2` 会返回 `manifest unknown`。因此 `.env` 中的 `WEKNORA_VERSION` 也必须写成 `v0.7.2`。

### 1. 在 Mac 上拉取 linux/amd64 镜像

> Mac 为 Apple Silicon 时，当前本地运行的镜像通常是 `linux/arm64`，不能直接导出给 `linux/amd64` 云服务器。即使本地已经有 `wechatopenai/*:latest`，也必须重新拉取目标版本和目标架构：

```bash
docker pull --platform linux/amd64 wechatopenai/weknora-ui:v0.7.2
docker pull --platform linux/amd64 wechatopenai/weknora-app:v0.7.2
docker pull --platform linux/amd64 wechatopenai/weknora-docreader:v0.7.2
docker pull --platform linux/amd64 paradedb/paradedb:v0.22.2-pg17
docker pull --platform linux/amd64 redis:7.0-alpine
```

### 2. 检查架构和远程摘要

所有镜像都必须显示 `linux/amd64`，并记录 `RepoDigests`，不要只依据可变标签判断内容：

```bash
docker image inspect \
  --format '{{.RepoTags}} {{.Os}}/{{.Architecture}} {{json .RepoDigests}}' \
  wechatopenai/weknora-ui:v0.7.2 \
  wechatopenai/weknora-app:v0.7.2 \
  wechatopenai/weknora-docreader:v0.7.2 \
  paradedb/paradedb:v0.22.2-pg17 \
  redis:7.0-alpine
```

### 3. 逐个保存镜像归档

为降低 Docker Desktop 导出大型合并归档时引擎中断或出现 `unexpected EOF` 的风险，逐个保存五个固定镜像。每个归档都只含 `linux/amd64` 平台，并立即进行 gzip 和 tar 两层完整性检查；必须启用 `pipefail`，否则上游 `docker image save` 失败可能被 gzip 的成功退出码掩盖：

```bash
set -o pipefail
docker image save --platform linux/amd64 wechatopenai/weknora-ui:v0.7.2 \
  | gzip -1 > weknora-ui-v0.7.2-linux-amd64.tar.gz
docker image save --platform linux/amd64 wechatopenai/weknora-app:v0.7.2 \
  | gzip -1 > weknora-app-v0.7.2-linux-amd64.tar.gz
docker image save --platform linux/amd64 wechatopenai/weknora-docreader:v0.7.2 \
  | gzip -1 > weknora-docreader-v0.7.2-linux-amd64.tar.gz
docker image save --platform linux/amd64 paradedb/paradedb:v0.22.2-pg17 \
  | gzip -1 > paradedb-v0.22.2-pg17-linux-amd64.tar.gz
docker image save --platform linux/amd64 redis:7.0-alpine \
  | gzip -1 > redis-7.0-alpine-linux-amd64.tar.gz
```

### 4. 完整性校验

```bash
for archive in \
  weknora-ui-v0.7.2-linux-amd64.tar.gz \
  weknora-app-v0.7.2-linux-amd64.tar.gz \
  weknora-docreader-v0.7.2-linux-amd64.tar.gz \
  paradedb-v0.22.2-pg17-linux-amd64.tar.gz \
  redis-7.0-alpine-linux-amd64.tar.gz
do
  gzip -t "$archive"
  gzip -dc "$archive" | tar -tf - >/dev/null
done

shasum -a 256 \
  weknora-ui-v0.7.2-linux-amd64.tar.gz \
  weknora-app-v0.7.2-linux-amd64.tar.gz \
  weknora-docreader-v0.7.2-linux-amd64.tar.gz \
  paradedb-v0.22.2-pg17-linux-amd64.tar.gz \
  redis-7.0-alpine-linux-amd64.tar.gz \
  > weknora-images-0.7.2-linux-amd64.sha256
```

### 5. 传输到服务器

关闭 VPN、确认本机 SSH 出口仍在云防火墙 TCP 22 的 `/32` 允许范围后，将归档和校验文件传到服务器。不要在聊天中发送 SSH 私钥或口令：

```bash
scp -i ~/.ssh/id_ed25519 \
  weknora-ui-v0.7.2-linux-amd64.tar.gz \
  weknora-app-v0.7.2-linux-amd64.tar.gz \
  weknora-docreader-v0.7.2-linux-amd64.tar.gz \
  paradedb-v0.22.2-pg17-linux-amd64.tar.gz \
  redis-7.0-alpine-linux-amd64.tar.gz \
  weknora-images-0.7.2-linux-amd64.sha256 \
  <用户名>@<云服务器公网IP>:/tmp/
```

### 6. 在服务器上导入镜像

```bash
cd /tmp
sha256sum -c weknora-images-0.7.2-linux-amd64.sha256
set -o pipefail
gzip -dc weknora-ui-v0.7.2-linux-amd64.tar.gz | sudo docker load
gzip -dc weknora-app-v0.7.2-linux-amd64.tar.gz | sudo docker load
gzip -dc weknora-docreader-v0.7.2-linux-amd64.tar.gz | sudo docker load
gzip -dc paradedb-v0.22.2-pg17-linux-amd64.tar.gz | sudo docker load
gzip -dc redis-7.0-alpine-linux-amd64.tar.gz | sudo docker load
```

### 7. 验证镜像

再次检查标签与架构：

```bash
sudo docker image inspect \
  --format '{{.RepoTags}} {{.Os}}/{{.Architecture}} {{json .RepoDigests}}' \
  wechatopenai/weknora-ui:v0.7.2 \
  wechatopenai/weknora-app:v0.7.2 \
  wechatopenai/weknora-docreader:v0.7.2 \
  paradedb/paradedb:v0.22.2-pg17 \
  redis:7.0-alpine
```

Mac 和服务器分别对每个镜像执行以下检查（服务器命令在 `docker` 前加 `sudo`），比较输出的 SHA-256：

```bash
docker image inspect --format '{{json .RootFS.Layers}}' \
  wechatopenai/weknora-ui:v0.7.2 | shasum -a 256
```

五个镜像的 RootFS 哈希都必须逐项相同；任何一项不一致都不要启动 Compose。

> 离线导入路径下不要执行 `docker compose pull`。确认五个镜像齐全后，后续启动命令使用 `--pull never`，防止 Compose 再次访问不可达的远程仓库。

### 8. 镜像拉取常见问题与排障（实战记录）

以下记录本次实际部署中遇到的问题与解决方案。

#### 问题 1：Mac 拉取 Docker Hub 超时

- 现象：`docker pull` 报 `dial tcp ...:443: i/o timeout` 或 `TLS handshake timeout`。
- 根因：Docker Desktop 默认直连 Docker Hub（国外），未走本地代理。
- 解决：Docker Desktop → Settings → Resources → Proxies，开启 Manual proxy configuration，HTTP 与 HTTPS 都填 `http://127.0.0.1:<代理端口>`，然后 Apply & Restart。
- 验证：浏览器访问 `http://127.0.0.1:<代理端口>` 显示"该网页无法正常运作"是**正常现象**（代理端口不是网页服务），说明代理在运行。

#### 问题 2：Apple Silicon Mac 拉取 `linux/amd64` 后架构仍不对

- 现象：`docker pull --platform linux/amd64` 返回成功，但 `docker image inspect` 的 `Architecture` 字段为空，或与 `arm64` 版本的 digest 相同。
- 根因：Docker Desktop 可能启用了「Use containerd for pulling and storing images」，镜像以 manifest list 形式存储，未展开具体平台，导致 `inspect` 读不到架构。
- 排查：`docker pull --platform linux/amd64` 未报 `no matching manifest for linux/amd64` 即证明远程有 amd64 版本。
- 解决：Docker Desktop → Settings → General，取消勾选「Use containerd for pulling and storing images」，Apply & Restart 后删除重拉。

#### 问题 3：服务器 sudo 密码失效

- 现象：重置密码后仍 `Permission denied`；`root` 密码登录被拒；sudo 输入密码报错。
- 根因：腾讯云轻量应用服务器 `root` 默认禁用密码登录（只允许密钥）；重置密码后**必须重启实例才生效**；此外常见误区是**在 Mac 终端执行 `sudo`**，此时输入的是 Mac 密码而非服务器密码（看提示符区分：`ubuntu@VM-...` 才是服务器终端，`lizi@...MacBook-Pro` 是 Mac 终端）。
- 解决：控制台重置 `ubuntu` 用户密码 → 重启实例 → 用密钥登录 `ubuntu` → 确认在服务器终端执行 `sudo` 并使用新密码。

#### 推荐路径：优先服务器直接拉，失败再 Mac 中转

服务器本身是 `linux/amd64`，直接拉就是 amd64 镜像，不存在架构混淆。建议先试服务器直接拉：

```bash
sudo tee /etc/docker/daemon.json >/dev/null <<'EOF'
{
  "registry-mirrors": ["https://mirror.ccs.tencentyun.com"]
}
EOF
sudo systemctl restart docker

sudo docker pull wechatopenai/weknora-ui:v0.7.2
sudo docker pull wechatopenai/weknora-app:v0.7.2
sudo docker pull wechatopenai/weknora-docreader:v0.7.2
sudo docker pull paradedb/paradedb:v0.22.2-pg17
sudo docker pull redis:7.0-alpine
```

验证（服务器上）：

```bash
sudo docker image inspect \
  --format '{{.RepoTags}} {{.Os}}/{{.Architecture}}' \
  wechatopenai/weknora-ui:v0.7.2 \
  wechatopenai/weknora-app:v0.7.2 \
  wechatopenai/weknora-docreader:v0.7.2 \
  paradedb/paradedb:v0.22.2-pg17 \
  redis:7.0-alpine
```

五个镜像都显示 `linux/amd64` 即成功。只有服务器确实拉不动（超时或 `MANIFEST_UNKNOWN`）时，才回到本章第 1-7 节的 Mac 离线中转方案。

---

## 五、获取代码并确定版本

### 1. 区分代码仓库与镜像仓库

- Git 代码仓库 `https://github.com/lizi586586-code/vidsage.git` 用来获取 `docker-compose.yml`、`.env.example` 和其他部署配置；
- 应用运行时的 `frontend`、`app` 和 `docreader` 从 `wechatopenai/*` 官方 Docker 镜像拉取；
- PostgreSQL 使用 `paradedb/paradedb:v0.22.2-pg17`，Redis 使用 `redis:7.0-alpine`；
- 仅克隆 Git 代码仓库不代表容器会根据该仓库的源码自动重新构建。

第一版没有修改应用源码，因此采用"自己的 Git 仓库中的部署配置 + 腾讯发布的固定版本镜像"。不在 2 vCPU / 4GB 云服务器上构建源码镜像。

> **如果计划修改 WeKnora 源码（二开个性化代码，例如新增视频页面、修改前端 UI、新增预加载 skills 等），请一律按 [《个性化代码部署与发布流程.md》](./个性化代码部署与发布流程.md) 执行。**
> 该文档完整覆盖第二次沟通 PDF §5.1/5.2/5.3/5.4 的全部要求：四分支 Git 工作流、少量多次 commit 规范、镜像 tag 与 commit 可追溯、复用 §4 离线中转操作、双文件 compose 启动（以及为什么单文件会 nginx upstream 断连）、三联一致性验收、个性化版本回滚、官方版本升级与 custom/main 的 rebase 衔接流程。

### 2. 克隆代码仓库

建议安装到固定路径：

```bash
sudo mkdir -p /opt/vidsage
sudo chown "$USER":"$USER" /opt/vidsage
git clone https://github.com/lizi586586-code/vidsage.git /opt/vidsage
cd /opt/vidsage
```

### 2.1 服务器访问 GitHub 失败的替代方案（实战记录）

国内服务器访问 GitHub 可能遇到两类失败：

- `Failed to connect to 127.0.0.1 port 7897`：服务器上残留了指向本地不存在代理的 git 配置。先清除代理：`git config --global --unset http.proxy && git config --global --unset https.proxy`。
- `GnuTLS recv error (-110)`：清除代理后直连 GitHub 的 TLS 连接被中断（网络不通）。

服务器确实拉不动 GitHub 时，改用「Mac 打包 → scp 传输 → 服务器解压」，不依赖服务器访问 GitHub：

```bash
# Mac 终端：打包（含 .git，压缩后约 150MB）
cd /Users/<你的路径>/Desktop/weknora
tar czf /tmp/vidsage-src.tar.gz vidsage

# Mac 终端：scp 传输（出现 password 提示时输入 ubuntu 密码）
scp -i ~/.ssh/vidsage.pem /tmp/vidsage-src.tar.gz ubuntu@<公网IP>:/tmp/

# 服务器终端：解压到 /opt/vidsage
sudo rm -rf /opt/vidsage
sudo tar xzf /tmp/vidsage-src.tar.gz -C /opt/
sudo chown -R "$USER":"$USER" /opt/vidsage
cd /opt/vidsage
git checkout custom/main
```

> `tar: Ignoring unknown extended header keyword 'LIBARCHIVE.xattr...'` 是 macOS 打包时的元数据提示，可忽略，不影响解压。

### 3. 切换到 custom/main 分支

```bash
git checkout custom/main
```

### 4. 记录部署版本

```bash
git status --short
git rev-parse HEAD
cat VERSION 2>/dev/null || echo "无 VERSION 文件"
```

### 5. 固定版本镜像（方式 A，本次使用）

在 `.env` 中把 `WEKNORA_VERSION` 固定为明确版本：

```dotenv
WEKNORA_VERSION=v0.7.2
```

> 不要在生产环境长期使用 `latest`，否则不同时间拉取的镜像可能不一致。

---

## 六、配置生产环境变量

### 1. 创建配置文件

```bash
cd /opt/vidsage
test -f .env || cp .env.example .env
chmod 600 .env
```

> `.env` 已被 Git 忽略，但它仍包含生产密钥。建议把密钥主副本保存在云厂商 Secret Manager/密钥管理服务中，部署时再写入 `/opt/vidsage/.env`，不要提交到代码仓库或聊天记录。

### 2. 生成密钥

```bash
DB_PASSWORD=$(openssl rand -base64 36 | tr -dc 'A-Za-z0-9' | head -c 24)
REDIS_PASSWORD=$(openssl rand -base64 36 | tr -dc 'A-Za-z0-9' | head -c 24)
JWT_SECRET=$(openssl rand -base64 64 | tr -dc 'A-Za-z0-9' | head -c 48)
SYSTEM_AES_KEY=$(openssl rand -base64 48 | tr -dc 'A-Za-z0-9' | head -c 32)
GRPC_AUTH_TOKEN=$(openssl rand -base64 36 | tr -dc 'A-Za-z0-9' | head -c 24)
```

> `SYSTEM_AES_KEY` 必须是刚好 32 个 ASCII 字符。一旦用于加密模型、MCP 或数据源凭据，就不能随意更换；丢失后数据库中的加密凭据无法恢复。

### 3. 最小生产配置

编辑 `.env`，至少确认以下配置：

```dotenv
# 固定 release 版本
WEKNORA_VERSION=v0.7.2
GIN_MODE=release
TZ=Asia/Shanghai

# 数据库
DB_DRIVER=postgres
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=<强随机密码>
DB_NAME=WeKnora

# Redis
REDIS_ADDR=redis:6379
REDIS_PASSWORD=<强随机密码>
STREAM_MANAGER_TYPE=redis

# 密钥
JWT_SECRET=<强随机密钥>
SYSTEM_AES_KEY=<刚好32个ASCII字符>
GRPC_AUTH_TOKEN=<强随机内部令牌>

# 存储
STORAGE_TYPE=local
RETRIEVE_DRIVER=postgres

# 第一版本暂不启用知识图谱及任何可选 profile
NEO4J_ENABLE=false
WEKNORA_SANDBOX_MODE=disabled

# 2 核 4G：降低文档处理与异步任务并发，优先保证稳定性
CONCURRENCY_POOL_SIZE=2
DOCREADER_GRPC_MAX_WORKERS=1
DOCREADER_MARKITDOWN_MAX_WORKERS=1
DOCREADER_PDF_RENDER_MAX_WORKERS=1
DOCREADER_PDF_RENDER_PARALLELISM=1
WEKNORA_ASYNQ_CORE_CONCURRENCY=2
WEKNORA_ASYNQ_POSTPROCESS_CONCURRENCY=1
WEKNORA_ASYNQ_ENRICHMENT_CONCURRENCY=2
WEKNORA_ASYNQ_MAINTENANCE_CONCURRENCY=1
WEKNORA_ASYNQ_SHARED_CONCURRENCY=1
WEKNORA_WIKI_ASYNQ_CONCURRENCY=1
WEKNORA_MODEL_MAX_CONCURRENCY=4

# 端口绑定（只在本机开放 app 健康检查，不暴露公网）
APP_PORT=127.0.0.1:8080
APP_BACKEND_PORT=8080
# 公网 IP 验证阶段使用 80；配置宿主机 HTTPS 代理时改成 127.0.0.1:8081
FRONTEND_PORT=80

# 首位管理员注册完成前保持 false，完成后改成 true
DISABLE_REGISTRATION=false
```

如果有单份 100MB 文件，再加入：

```dotenv
MAX_FILE_SIZE_MB=120
DOCREADER_GRPC_MAX_FILE_SIZE_MB=120
```

配置正式域名后补充：

```dotenv
FRONTEND_BASE_URL=https://kb.example.com
APP_EXTERNAL_URL=https://kb.example.com
```

> 模型 API Key 可以在首次登录后的初始化向导中配置，也可以参考 `BUILTIN_MODELS.md` 使用声明式配置。不要把真实 API Key 提交到 Git。

### 3.5 一键配置脚本（实战验证，推荐）

以下脚本一次完成「复制 .env → 生成随机密钥 → 替换关键字段 → 追加并发限制」，避免手动编辑 vim：

```bash
cd /opt/vidsage
test -f .env || cp .env.example .env
chmod 600 .env

DB_PASSWORD=$(openssl rand -base64 36 | tr -dc 'A-Za-z0-9' | head -c 24)
REDIS_PASSWORD=$(openssl rand -base64 36 | tr -dc 'A-Za-z0-9' | head -c 24)
JWT_SECRET=$(openssl rand -base64 64 | tr -dc 'A-Za-z0-9' | head -c 48)
SYSTEM_AES_KEY=$(openssl rand -base64 48 | tr -dc 'A-Za-z0-9' | head -c 32)

sed -i "s|^WEKNORA_VERSION=.*|WEKNORA_VERSION=v0.7.2|" .env
sed -i "s|^DB_PASSWORD=.*|DB_PASSWORD=${DB_PASSWORD}|" .env
sed -i "s|^REDIS_PASSWORD=.*|REDIS_PASSWORD=${REDIS_PASSWORD}|" .env
sed -i "s|^JWT_SECRET=.*|JWT_SECRET=${JWT_SECRET}|" .env
sed -i "s|^SYSTEM_AES_KEY=.*|SYSTEM_AES_KEY=${SYSTEM_AES_KEY}|" .env
sed -i "s|^APP_PORT=.*|APP_PORT=127.0.0.1:8080|" .env
sed -i "s|^CONCURRENCY_POOL_SIZE=.*|CONCURRENCY_POOL_SIZE=2|" .env

cat >> .env <<'EOF'

# ==== 2核4G 并发限制（vidsage 生产调优）====
DOCREADER_GRPC_MAX_WORKERS=1
DOCREADER_MARKITDOWN_MAX_WORKERS=1
DOCREADER_PDF_RENDER_MAX_WORKERS=1
DOCREADER_PDF_RENDER_PARALLELISM=1
WEKNORA_ASYNQ_CORE_CONCURRENCY=2
WEKNORA_ASYNQ_POSTPROCESS_CONCURRENCY=1
WEKNORA_ASYNQ_ENRICHMENT_CONCURRENCY=2
WEKNORA_ASYNQ_MAINTENANCE_CONCURRENCY=1
WEKNORA_ASYNQ_SHARED_CONCURRENCY=1
WEKNORA_WIKI_ASYNQ_CONCURRENCY=1
WEKNORA_MODEL_MAX_CONCURRENCY=4
EOF

echo "===== 关键密钥（请截图备份）====="
echo "DB_PASSWORD=${DB_PASSWORD}"
echo "REDIS_PASSWORD=${REDIS_PASSWORD}"
echo "JWT_SECRET=${JWT_SECRET}"
echo "SYSTEM_AES_KEY=${SYSTEM_AES_KEY}"
```

> 执行后终端会打印 4 个密钥，务必截图保存（尤其 `SYSTEM_AES_KEY` 丢失不可恢复）。验证可用 `grep -E '^(WEKNORA_VERSION|DB_PASSWORD|REDIS_PASSWORD|JWT_SECRET|SYSTEM_AES_KEY|APP_PORT|CONCURRENCY_POOL_SIZE)=' .env`。

### 4. 校验配置

```bash
sudo docker compose config --quiet
sudo docker compose config --services
```

默认服务列表应为：

```text
postgres
redis
docreader
app
frontend
```

---

## 七、为 Redis 增加持久化卷

当前标准 Compose 为 Redis 开启了 AOF，但没有给 `/data` 挂载命名卷。容器被删除并重建时，队列和流状态可能丢失。云部署还需要再次固定镜像，并为 2 核 4GB 主机设置容器资源上限。增加以下 Compose override：

```bash
cd /opt/vidsage
tee docker-compose.cloud.yml >/dev/null <<'EOF'
services:
  frontend:
    image: wechatopenai/weknora-ui:v0.7.2
    cpus: "0.25"
    mem_limit: 128m
    pids_limit: 128
  app:
    image: wechatopenai/weknora-app:v0.7.2
    cpus: "1.00"
    mem_limit: 768m
    pids_limit: 512
  docreader:
    image: wechatopenai/weknora-docreader:v0.7.2
    cpus: "1.00"
    mem_limit: 1152m
    pids_limit: 512
  postgres:
    image: paradedb/paradedb:v0.22.2-pg17
    cpus: "0.75"
    mem_limit: 896m
    pids_limit: 256
    shm_size: 128m
  redis:
    image: redis:7.0-alpine
    cpus: "0.25"
    mem_limit: 192m
    pids_limit: 128
    volumes:
      - redis-data:/data
volumes:
  redis-data:
EOF
```

后续所有 Compose 命令都同时指定两个文件：

```bash
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml config --quiet
```

> Redis 持久化不能替代备份。核心业务数据仍以 PostgreSQL、`data-files` 卷和 `SYSTEM_AES_KEY` 为主。

---

## 八、启动 5 个核心服务

```bash
cd /opt/vidsage
```

使用已经离线导入服务器的官方固定版本镜像。不要再次拉取，也不要触发源码构建：

```bash
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml \
  up -d --pull never --no-build
```

> 首次启动如果健康检查失败，先停止五个服务并保留容器、镜像和全部命名卷，修正配置后再重新执行上面的 `up` 命令：
>
> ```bash
> sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml \
>   stop frontend app docreader redis postgres
> ```
>
> **绝对禁止执行 `docker compose down -v`**；该命令会删除 PostgreSQL、上传文件和 Redis 等命名卷中的数据。

检查状态：

```bash
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml ps
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml logs --tail=100 app docreader postgres redis frontend
curl -f http://127.0.0.1:8080/health
curl -I http://127.0.0.1/
```

预期：
- `app`、`docreader`、`postgres` 显示 healthy；
- `redis`、`frontend` 显示 running；
- `/health` 返回成功；
- 前端返回 HTTP 200 或正常跳转。

---

## 九、通过公网 IP 验证

确认安全组已开放 80 后，在自己的电脑浏览器访问：

```text
http://<云服务器公网IP>/
```

> 注意：远程用户访问时不能填写 `localhost`，因为 `localhost` 指向用户自己的电脑。

首次验证至少完成：
- 打开注册/登录页面；
- 注册首位管理员；
- 完成模型、Embedding 和可选 Rerank 配置；
- 创建知识库；
- 上传一份小文档并等待解析完成；
- 发起一次问答并确认 SSE 流式输出正常；
- 重启服务后确认数据仍在。

### 1. 登录卡住/提示"网络错误"排障（实战记录）

- 现象：浏览器打开 `http://<公网IP>/login` 输入账号密码点登录后，按钮转圈后停在登录页不跳转，提示"网络错误"；Network 里只有 `login` 请求（200），没有后续 `auth/me` 请求；localStorage 里没有 token。服务器侧 curl 直连 login 接口却返回 200 正常 JSON。
- 根因：本机（Mac）的 Clash Verge 系统代理拦截了浏览器到服务器的流量，登录成功响应（较大 gzip 包）在代理这一跳被截断，前端 axios 判定"没有收到响应"（`error.response` 为空）→ 报"网络错误"。curl 默认**不读** macOS 系统代理，所以直连一直正常，形成"浏览器坏、curl 好"的假象。服务器、后端、前端代码均无问题。
- 排查要点：
  - 浏览器 Network 里看 login 响应的**响应头**：出现 `Proxy-Connection: keep-alive` / `Keep-Alive: timeout=4` 就是本地代理注入的（直连 nginx 不会有这两个头）；
  - `scutil --proxy` 查看 macOS 系统代理设置（本例为 `127.0.0.1:7897`）；
  - 对比 `curl -x <代理> ...` 与直连 curl 的响应头差异。
- 解决（二选一）：
  - 临时：关闭 Clash Verge 的"系统代理"开关；
  - 长期：Clash Verge → 订阅卡片 → 扩展配置（Extend Config）里加直连规则，让服务器流量不经过代理：
    ```yaml
    prepend-rules:
      - IP-CIDR,42.194.211.189/32,DIRECT,no-resolve
    ```
    保存后重新激活订阅生效。验证：Clash 连接页中该 IP 的 Rule 显示 `DIRECT`，重新登录正常跳转。
- 经验：浏览器和 curl 结果不一致时，第一时间怀疑本地代理——curl 默认不走系统代理，浏览器会走。

### 2. 系统管理员提升（实战记录）

系统里有两层"管理员"，容易混淆：

- **工作空间角色（Owner/Admin/...）**：首位用户注册时自动获得个人工作空间的 `owner` 角色，对知识库、模型配置、成员邀请拥有全部权限，日常使用足够。
- **平台系统管理员（`users.is_system_admin`）**：负责用户管理（重置密码、设管理员）、跨工作空间操作、内置模型管理、平台 API Key 与系统设置。

注意：当前代码**没有"首个用户自动成为系统管理员"的引导逻辑**——`Register` 不设置该字段，`/auth/auto-setup` 仅限 lite edition（完整版返回 403），而 promote 接口又要求调用者已是系统管理员，形成死锁。需要提升时只能直接改数据库：

```bash
cd /opt/vidsage
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml exec -T postgres \
  psql -U postgres -d WeKnora \
  -c "UPDATE users SET is_system_admin = true WHERE email = '<管理员邮箱>';"
```

验证（末尾为 `t` 即成功）：

```bash
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml exec -T postgres \
  psql -U postgres -d WeKnora -tAc "SELECT username, email, is_system_admin FROM users;"
```

鉴权中间件每次请求都从数据库读取用户信息，改完立即生效，无需重启容器；如前端入口未出现，退出重新登录即可。

首位管理员创建完成后，修改 `.env`：

```dotenv
DISABLE_REGISTRATION=true
```

应用配置：

```bash
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml up -d app
```

---

## 十、配置域名与 HTTPS

> 域名备案完成后执行本节。

正式使用不要长期停留在公网 IP + HTTP。可以选择云负载均衡/应用网关托管证书，也可以在宿主机安装 NGINX。下面给出宿主机 NGINX 方案。

### 1. 把 WeKnora 前端改为仅本机监听

修改 `.env`：

```dotenv
FRONTEND_PORT=127.0.0.1:8081
FRONTEND_BASE_URL=https://kb.example.com
APP_EXTERNAL_URL=https://kb.example.com
```

重新创建 frontend 和 app：

```bash
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml up -d frontend app
```

确认：

```bash
curl -I http://127.0.0.1:8081/
```

### 2. 安装宿主机 NGINX 与 Certbot

```bash
sudo apt update
sudo apt install -y nginx certbot python3-certbot-nginx
```

### 3. 创建 NGINX 配置

创建 `/etc/nginx/sites-available/vidsage.conf`：

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name kb.example.com;
    # 单份文件可能达到 100MB 时使用 120m；否则可保持 50m。
    client_max_body_size 120m;
    location / {
        proxy_pass http://127.0.0.1:8081;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # WeKnora 聊天使用 SSE，外层代理也不能缓冲。
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
```

启用并检查：

```bash
sudo ln -sfn /etc/nginx/sites-available/vidsage.conf /etc/nginx/sites-enabled/vidsage.conf
sudo nginx -t
sudo systemctl reload nginx
```

### 4. 申请 HTTPS 证书

DNS 已指向当前服务器后申请证书：

```bash
sudo certbot --nginx -d kb.example.com
```

验证：

```bash
curl -I https://kb.example.com/
sudo certbot renew --dry-run
```

最终用户统一访问：

```text
https://kb.example.com/
```

---

## 十一、上线验收

### 服务验收

- [ ] `docker compose ps` 中 5 个核心服务正常；
- [ ] `curl http://127.0.0.1:8080/health` 成功；
- [ ] 公网 HTTPS 首页可以访问；
- [ ] HTTP 自动跳转 HTTPS；
- [ ] 用户不能从公网连接 8080、50051、5432、6379；
- [ ] 首位管理员已创建，公开注册已关闭；
- [ ] 模型、Embedding、Rerank 连通性测试成功；
- [ ] 文档上传、解析、检索和问答成功；
- [ ] SSE 回答不会等待整段完成后一次性出现。
- [ ] 上传一份有代表性的文档时观察 `docker stats`，内存没有持续超过 85%；
- [ ] `swapon --show` 正常，解析完成后 Swap 没有持续大量增长；

### 安全验收

- [ ] SSH 仅允许管理员固定 IP，优先使用密钥登录；
- [ ] `.env` 权限为 600，且未进入 Git；
- [ ] 生产镜像使用明确版本或 commit 标签，不使用可漂移的 `latest`；
- [ ] `SYSTEM_AES_KEY` 已备份到 Secret Manager；
- [ ] 数据库与 Redis 使用强随机密码；
- [ ] 仅开放 80/443，内部端口未暴露；
- [ ] 云监控已配置 CPU、内存、磁盘和实例不可达告警。

---

## 十二、备份与恢复

至少备份三类内容：
1. PostgreSQL 数据；
2. `data-files` 上传文件卷；
3. `.env` 中的关键密钥，尤其 `SYSTEM_AES_KEY`。

### 1. PostgreSQL 逻辑备份

```bash
cd /opt/vidsage
sudo install -d -m 0700 -o "$USER" -g "$USER" /opt/vidsage-backups
BACKUP_DIR=/opt/vidsage-backups
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml exec -T postgres \
  sh -c 'pg_dump -U "$POSTGRES_USER" "$POSTGRES_DB"' \
  | gzip > "${BACKUP_DIR}/postgres-$(date +%F-%H%M%S).sql.gz"
```

### 2. 上传文件备份

```bash
BACKUP_DIR=/opt/vidsage-backups
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml exec -T app \
  tar czf - -C /data files \
  > "${BACKUP_DIR}/data-files-$(date +%F-%H%M%S).tar.gz"
```

### 3. 密钥备份

把以下值保存到云 Secret Manager，不要只保留在服务器磁盘：
- `DB_PASSWORD`
- `REDIS_PASSWORD`
- `JWT_SECRET`
- `SYSTEM_AES_KEY`
- 模型与第三方服务 API Key

> 建议每日数据库备份、每日或每周文件备份，并把备份复制到与云主机不同故障域的对象存储。至少每季度做一次恢复演练；未经恢复验证的备份不能视为有效备份。

### 4. 恢复演练

恢复会覆盖现有数据，必须先确认备份文件、进入维护窗口并阻止用户写入。先停止业务服务，保留 PostgreSQL 和 Redis：

```bash
cd /opt/vidsage
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml stop frontend app docreader
```

恢复 PostgreSQL 到空数据库：

```bash
DB_BACKUP=/opt/vidsage-backups/postgres-YYYY-MM-DD-HHMMSS.sql.gz
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml exec -T postgres \
  sh -c 'dropdb -U "$POSTGRES_USER" --if-exists "$POSTGRES_DB" && createdb -U "$POSTGRES_USER" "$POSTGRES_DB"'
gzip -dc "$DB_BACKUP" \
  | sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml exec -T postgres \
    sh -c 'psql -v ON_ERROR_STOP=1 -U "$POSTGRES_USER" "$POSTGRES_DB"'
```

恢复上传文件卷：

```bash
FILE_BACKUP=/opt/vidsage-backups/data-files-YYYY-MM-DD-HHMMSS.tar.gz
gzip -dc "$FILE_BACKUP" \
  | sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml run --rm -T --no-deps \
    --entrypoint tar app xvf - -C /data
```

确认 `.env` 中的 `SYSTEM_AES_KEY` 与备份时一致，再启动并验收：

```bash
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml up -d
curl -f http://127.0.0.1:8080/health
```

---

## 十三、升级与回滚

### 1. 升级前

```bash
cd /opt/vidsage
git rev-parse HEAD
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml images
```

升级前必须：
- 完成 PostgreSQL、`data-files` 和密钥备份；
- 记录当前 Git commit 与镜像标签；
- 阅读目标版本变更记录；
- 先在测试环境验证启动、迁移、上传和问答。

### 2. 固定版本镜像升级

修改 `.env` 中的 `WEKNORA_VERSION` 为目标版本，然后：

```bash
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml pull
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml up -d
sudo docker compose -f docker-compose.yml -f docker-compose.cloud.yml ps
curl -f http://127.0.0.1:8080/health
```

### 3. 回滚

如果新版本尚未执行不兼容数据库迁移：
1. 把 `.env` 中 `WEKNORA_VERSION` 改回旧版本；
2. 重新执行 `docker compose up -d`；
3. 验证健康检查、登录、检索、上传和问答。

如果数据库结构已经发生不兼容变化，仅回滚镜像可能不足。应在维护窗口中停止写入，恢复升级前的 PostgreSQL 和文件备份，再启动旧版本。

> **个性化二开代码版本（custom tag 粒度）的升级与回滚**不属于本章节「改 `.env` 的 `WEKNORA_VERSION` + `docker compose pull`」官方版本升级范畴，请参见 [《个性化代码部署与发布流程.md》§5 个性化版本的升级 / 回滚](./个性化代码部署与发布流程.md#5-个性化版本的升级--回滚) 与 §6 官方版本升级与 custom/main 衔接流程。

---

## 十四、安全红线

1. **绝对禁止 `docker compose down -v`** — 会删除所有命名卷（PostgreSQL、上传文件、Redis），数据不可恢复。
2. **镜像标签带 `v`** — 用 `v0.7.2` 不是 `0.7.2`，不要用 `latest`。
3. **`.env` 权限 600** — 包含生产密钥，不能进入 Git，不能出现在聊天记录中。
4. **`SYSTEM_AES_KEY` 不可丢失** — 一旦用于加密凭据，丢失后数据库中的加密凭据无法恢复。
5. **内部端口不暴露公网** — 8080、50051、5432、6379 仅本机访问。
6. **SSH 限管理员 IP** — TCP 22 仅允许管理员固定 IP，优先密钥登录。
