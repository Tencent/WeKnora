# WeKnora 问题与待确认项（2026-07-10）

## 一、审计范围与当前结论

- 仓库：Tencent/WeKnora
- 基线：`main@37dcdc7f74b544bf1aed347a01f1a6592f3210d1`
- 参考：`Comdoc/` 4 份需求/后端设计文档的“概述—流程—规则—异常—关系—验收”结构
- 审计口径：30 个业务域、独立子工程与工程基础设施模块
- 默认运行路径：Docker Compose 核心服务；源码健康度另用各子工程原生测试/构建验证

## 二、需要项目负责人确认的问题

| 编号 | 问题 | 为什么需要确认 | 当前采用的安全默认值 | 影响模块 |
|---|---|---|---|---|
| Q-01 | 最终目标环境是个人本地、团队内网还是公网生产？ | 决定 TLS、反向代理、域名、CORS、密钥托管、备份与高可用要求 | 仅按本机核心服务验证，不声明可直接公网生产 | Docker/Helm、Auth、Embed、存储 |
| Q-02 | 后续部署应固定提交 SHA、发布版本还是继续使用浮动 `latest` tag？ | 浮动 tag 不利于复现、升级和回滚 | 本次从 `main@37dcdc7f` 工作区源码构建本地 `latest` 镜像；后续生产建议固定不可变版本 | 全局、Config |
| Q-03 | 首选 LLM、Embedding、Rerank、VLM、ASR 供应商与模型是什么？ | 没有模型凭证仍可启动平台，但不能完成真实 RAG/Agent 业务验收 | 不写任何第三方 Key；本次不验收模型管理和 RAG 业务，仅确认无模型凭证时核心平台可启动 | 模型、RAG、Agent、解析 |
| Q-04 | 是否启用 Neo4j、MinIO、Langfuse、SearXNG、Qdrant/Milvus/Weaviate/Doris 等可选 profile？ | 会显著增加资源、端口、密码与运维复杂度 | 只启用默认核心服务；本地文件存储；Langfuse 关闭 | Wiki、存储、检索、观测 |
| Q-05 | 是否允许用户自助注册，RBAC 是否必须强制开启？ | 影响租户隔离、首次管理员流程与暴露面 | 保留上游默认 RBAC 行为，不设置 bootstrap 管理员邮箱 | Auth、Tenant、System Admin |
| Q-06 | 数据保留、备份、恢复时间和审计日志保留期要求是什么？ | PostgreSQL、文件卷、Redis、Wiki DLQ 需要一致的恢复策略 | 本地卷仅用于验证，不承诺生产持久性 | 数据库、存储、审计、队列 |
| Q-07 | 是否需要接入 Feishu/Notion/Yuque/RSS 与哪些 IM 渠道？ | 每个连接器都需要独立凭证、权限范围、回调与限流验收 | 不启用外部连接器，不填凭证 | 数据源、IM |
| Q-08 | Website Embed 是否面向外部域名，是否使用 secure mode？ | 需确定域名白名单、真实客户端 IP、CSP、token exchange 和限流 | 不开放外部嵌入渠道 | Embed、Nginx、Auth |
| Q-09 | 需要支持哪些文档格式与单文件大小/并发上限？ | 决定 DocReader 依赖、OCR/VLM、资源配额与测试语料 | 默认 50 MB、默认解析器、基础健康验证 | DocReader、上传、模型 |
| Q-10 | 部署升级策略是否允许自动迁移，必须支持哪些历史版本升级？ | 当前已有 60+ 版本化迁移，生产升级需备份和回滚策略 | 仅验证全新数据库初始化 | Migration、Database |
| Q-11 | CLI、Go SDK、MCP Server、微信小程序是否属于本次正式交付面？ | 它们各有独立版本、依赖和兼容契约 | 做静态/单元验证，但不把外部渠道登录作为核心启动门禁 | CLI、client、MCP、小程序 |
| Q-12 | 期望的容量和 SLO（文档量、并发问答、P95、恢复时间）是什么？ | 决定并发限制、队列 worker、向量索引和资源规格 | 使用上游默认并发，仅做单机 smoke test | 模型限流、队列、检索、部署 |

## 三、代码与文档中已发现的问题

| 编号 | 严重度 | 现象与证据 | 建议 |
|---|---|---|---|
| F-01 | 高 | `.env.example` 中 `LANGFUSE_PUBLIC_KEY/SECRET_KEY` 是非空占位值，而代码说明“两者非空即自动启用” | 示例 Key 默认留空；Cloud 模式填写真实 Key 与云端 Host，自建模式启用 `langfuse` profile 后填写 Key 与内部 Host |
| F-02 | 高 | `scripts/check-env.sh` 直接 `source .env`，会把不可信 `.env` 当 Shell 执行，并把 DB/Redis 密码原值打印到终端 | 改用 dotenv 解析与 allowlist，只输出 `SET/UNSET`，禁止执行内容或显示秘密 |
| F-03 | 中 | `helm/values.yaml` 的 ParadeDB 部署默认值及 `helm/README.md` 示例均为 `v0.18.9-pg17`，Compose 使用 `v0.22.2-pg17` | 从单一版本源生成 Helm/Compose 配置与文档，并在 CI 检查漂移和迁移兼容 |
| F-04 | 中 | `mcp-server/pyproject.toml` 项目 URL 指向第三方 `NannaOlympicBroadcast/WeKnoraMCP` | 校正为 Tencent/WeKnora 的模块路径与 issue 地址 |
| F-05 | 中 | MCP Server pytest 配置 `testpaths=["tests"]`，实际测试文件位于模块根目录 | 移入 `tests/` 或修正 testpaths，并加入根级 verify |
| F-06 | 低 | `frontend/package.json` 名称为 `knowledage-base` | 更正拼写并确认不会影响构建/缓存标识 |
| F-07 | 低 | `docreader/pyproject.toml` description 是模板占位语句 | 补充真实模块描述、支持格式与服务边界 |
| F-08 | 中 | 根仓库没有统一验证命令覆盖三个 Go module、前端、Python、小程序、Compose 与 Helm | 增加只读/无凭证可运行的 `make verify` 聚合门禁 |
| F-09 | 中 | `internal/container/container.go` 与 `internal/router/router.go` 是高耦合组合点，业务扩展会继续放大 | 按稳定模块拆注册文件，保留统一构建与 RBAC 自检 |
| F-10 | 中 | 前端 `KnowledgeBaseList.vue` 的 `isWikiKb` 弱类型与本地 KB 类型无字段重叠，`vue-tsc` 报两处 `TS2559` | 已改为对 `unknown` 做安全运行时收窄；类型检查、生产构建和 157 个测试均通过 |
| F-11 | 中 | Python MCP Server 采用 `py-modules`，但根 `__init__.py` 使用无 package anchor 的相对导入，pytest setup 失败 | 已改为顶层模块导入；pytest 10 个测试通过，保留测试布局/返回值告警作为后续优化 |
| F-12 | 低 | Standard 版首次打开登录页仍请求 Lite-only `/auth/auto-setup`，预期 403 会在浏览器控制台显示为资源错误 | 建议由公开 auth config 返回 auto-setup capability，前端仅在 Lite 调用；涉及公共响应契约，未在本次擅自扩张 |
| F-13 | 低 | Notion 与远程图片测试依赖 loopback HTTP server，但 SSRF 白名单缓存未在测试内隔离，导致完整测试在默认环境失败 | 已在对应测试中显式设置并清理 loopback 白名单与缓存；生产 SSRF 默认策略未放宽，完整 Go 测试通过 |

## 四、运行阶段问题记录

### R-01：Docker daemon 未启动

- 现象：Docker CLI/Compose 存在，但连接 `/Users/xiaoxie/.docker/run/docker.sock` 失败。
- 根因：Docker Desktop 未启动，不是仓库代码问题。
- 处理：启动已安装的 Docker Desktop；随后 `docker info` 成功，显示 aarch64、10 CPU、约 8 GB 内存。
- 状态：已解决。

### R-02：ParadeDB 镜像首次拉取中断

- 现象：`docker compose up -d` 拉取 `paradedb/paradedb:v0.22.2-pg17` blob 时 Docker Hub 返回 `EOF`，容器尚未创建。
- 根因假设：远端镜像层传输中断；Compose 配置已独立解析通过。
- 处理：只重试失败镜像，成功后再恢复整体启动，避免同时改配置。
- 状态：已解决；单镜像重试成功。

### R-03：宿主机没有 Go

- 现象：`go list ./...` 返回 `command not found: go`。
- 影响：不能直接在宿主机运行 Go 测试。
- 处理：不擅自安装系统依赖，使用与 `go.mod` 对齐的 Go 容器执行测试/构建。
- 状态：验证阻塞已解决；宿主机仍未安装 Go，后续继续使用容器化 Go 1.26 验证。App 源码镜像构建成功，根模块、`client` 与 `cli` 的完整 Go 测试均已通过。

### R-04：App 首次构建时 Go Proxy 中断

- 现象：`go mod download` 在约 394 秒后收到 `proxy.golang.org` 的 HTTP/2 `GOAWAY server_shutting_down`。
- 根因：远端 Go Proxy 连接终止；没有编译错误，BuildKit 已保存依赖缓存。
- 处理：仅重试 App 构建，缓存命中后依赖下载 12.3 秒完成，随后生产二进制和镜像构建成功。
- 状态：已解决。

### R-05：Standard 版首屏 Lite 探测返回 403

- 现象：真实浏览器首次访问 `/login` 时页面完整渲染，但控制台记录 `/api/v1/auth/auto-setup` 403。
- 根因：登录页为兼容 Lite 版先调用 auto-setup；Standard 后端按设计拒绝并返回 403，前端捕获后进入普通登录。
- 影响：不影响页面或 API 健康；该请求未产生后端 `ERROR` 级日志，但使首次浏览器控制台不为零错误。
- 状态：作为 F-12 记录；修复需要扩展公开 auth config 的 capability 字段，属于公共响应契约变更。

### R-06：受控 Go 集成测试触发 DocReader SSRF 拒绝日志

- 现象：DocReader 在 `13:25:56` 记录两条 `ERROR`，内容为访问 `example.com` 时解析到本机代理保留网段 `198.18.0.0/15`，被 SSRF guard 拒绝。
- 根因：完整 Go 测试通过临时端口转发调用运行中的 DocReader；这是错误路径测试产生的受控请求，不是后台持续故障。
- 影响：容器持续健康、restart 为 0；该历史日志保留用于追溯，不能宣称容器整个生命周期“零 ERROR”。
- 状态：已解释；最终验收改为检查该时间点之后是否出现新的、未解释的错误。

## 五、验收前必须关闭的事项

1. 核心 5 个容器启动且健康检查收敛，无 restart loop。
2. Web UI 与 `/health` 均返回成功；日志不存在未解释的 panic/fatal/持续 ERROR。
3. 当前源码的 Go、前端、Python/小程序可执行测试完成，或记录明确且不可规避的外部依赖。
4. `Config.md` 不含真实秘密，所有实际配置差异、可选配置和验证命令完整。
5. 本文、`2026-07-10_Need.md`、`Config.md` 各自取得独立子智能体审批，阻塞意见已修订。
