# WeKnora 模块功能、用途与优化需求（2026-07-10）

## 一、目标与总体架构

WeKnora 是面向企业知识管理的自托管 RAG/Agent/Wiki 平台。核心链路为：内容经数据源或上传进入 DocReader，完成解析、切分与多模态后处理；Embedding 写入可替换向量库；问答或 Agent 将知识检索、Rerank、Web Search、MCP 工具和模型推理组合为流式回答；前端、CLI、SDK、MCP Server、微信小程序与 IM/Embed 渠道共享后端能力。

本次以 `main@37dcdc7f` 为代码基线，按 30 个稳定模块说明入口、功能、用途、依赖与优化方向。状态口径：表中内容是“现状能力 + 后续建议”；只有明确写“本次已修复”的内容已落地。优先级遵循最小充分原则：P0 影响安全/可运行性，P1 影响质量/维护性，P2 属于体验或工程演进。

## 二、模块功能、用途与优化清单

| # | 模块与代码入口 | 功能与用处 | 关键依赖 | 建议优化 | 优先级 |
|---|---|---|---|---|---|
| 1 | 服务入口/API：`cmd/server/`、`internal/router/`、`internal/handler/` | 启动 Gin，提供健康检查、REST/SSE、Swagger；含 Initialization/System/SystemAdmin/WeKnoraCloud 路由 | Container、中间件、应用服务 | 按业务域拆路由；保留统一 RBAC 自检 | P1 |
| 2 | 配置/DI/Runtime：`config/`、`internal/config/`、`internal/container/`、`internal/runtime/` | 装配数据库、Redis、模型、检索和外部客户端；承载 DB-backed 系统设置与生命周期 | 几乎全部后端模块 | 配置 schema、脱敏启动检查；拆分组合根 | P0/P1 |
| 3 | Auth/Tenant/RBAC/Organization：`internal/middleware/`、`internal/application/service/{user,tenant,organization,audit_log}*` | JWT/OIDC/API Key、租户角色、邀请、共享、审计、系统管理员与初始化权限 | DB、Redis、JWT/AES、路由策略 | 权限矩阵契约测试；密钥轮换和审计保留检查 | P0 |
| 4 | 知识资产：`internal/application/service/knowledge*`、`knowledgebase*`、`tag.go`、`file/` | 知识库、文档、FAQ、标签、收藏、上传/复制/移动/重解析/批处理 | DocReader、存储、队列、向量库、模型 | 明确状态机、幂等与补偿；统一部分成功语义 | P1 |
| 5 | 解析/切分/后处理：`internal/infrastructure/{docparser,chunker}/`、`knowledge_process*` | 文档抽取、chunk、摘要、问题、图片描述、图谱抽取 | DocReader、VLM/ASR/Embedding、存储、队列 | 单一解析状态模型；格式语料与性能基线 | P1 |
| 6 | 检索/向量库/融合：`internal/application/service/retriever/`、`internal/searchutil/` | BM25、Dense、Graph、父子块及多向量库融合 | Embedding/Rerank、数据库驱动、知识库配置 | 驱动能力矩阵与契约测试；可选驱动延迟加载 | P1 |
| 7 | 模型/供应商/Cloud：`internal/models/`、`config/builtin_models.yaml.example`、WeKnoraCloud service/handler | LLM/Embedding/Rerank/VLM/ASR 传输、凭证、并发、超时及 Cloud 凭证/状态 | 外部 API/Ollama、Langfuse、AES | 结构化能力声明；统一重试、错误码与指标 | P1 |
| 8 | RAG 会话/消息：`internal/application/service/{session,message,chat_pipeline}*`、`internal/stream/` | 检索增强问答、SSE、引用、进度与历史 | 知识库、模型、检索、Redis stream | 版本化 SSE；取消、断线与部分失败 E2E | P1 |
| 9 | Agent/Tools/Skills：`internal/agent/`、`agent*`、`skill_service.go`、`internal/sandbox/` | 多步推理、工具、记忆、自定义 Agent、沙箱与审批 | 模型、MCP、搜索、检索、Redis、Docker | 统一权限/配额/审计 policy；解耦可选沙箱 | P0/P1 |
| 10 | Wiki/知识图谱：`internal/application/service/wiki_*`、`memory/`、`graph.go` | 自动生成 Wiki、层级、引用、链接、lint 与图谱 | Agent/LLM、队列、DB、可选 Neo4j | checkpoint/DLQ 管理 UI；源文档一致性策略 | P1 |
| 11 | 数据源：`internal/datasource/connector/{feishu,notion,yuque,rss}/` | 外部内容源全量/增量同步及同步日志 | 外部 API、加密凭证、知识处理管线 | cursor/限流/删除/幂等规范；mock 契约测试 | P1 |
| 12 | IM：`internal/im/{dingtalk,feishu,mattermost,qqbot,slack,telegram,wechat,wecom}/` | 企业与即时消息渠道、文件、会话映射和长连接 | 平台凭证、会话/Agent、流式事件 | 能力矩阵；统一验签、防重放、重连指标 | P0/P1 |
| 13 | MCP 集成：`internal/mcp/`、`internal/application/service/mcp_*` | MCP 服务、工具/资源、OAuth2 与人工审批 | Agent、AES、OAuth、Redis | capability 漂移检测；审批 request/decision ID | P1 |
| 14 | Web Search/抓取：`internal/infrastructure/{web_search,web_fetch}/` | 多搜索供应商与网页获取，供 Agent 调用 | 外部服务、SSRF、Agent | 结果去重/可信度/限速；SSRF 安全回归 | P0/P1 |
| 15 | Website Embed：`internal/application/service/embed_*`、`internal/handler/embed_*`、`frontend/src/views/embed/` | 外站嵌入、域名白名单、secure mode 与限流 | Agent、会话、JWT/签名、真实客户端 IP | CSP/CORS/代理模板；安全失败指标 | P0 |
| 16 | 评测/数据集：`internal/application/service/{dataset,evaluation}.go`、`dataset/` | QA 采样/生成、召回与 BLEU/ROUGE 指标 | Pandas/Parquet、模型、检索 | 独立 lockfile；固定小样本 CI smoke | P1 |
| 17 | Web 前端：`frontend/` | Vue/Pinia/TDesign 管理知识、聊天、Agent 与平台设置 | 后端 REST/SSE、Node/npm | 本次修复 Wiki 判断类型；默认类型构建、流程测试、包名纠正 | P1/P2 |
| 18 | Go SDK：`client/` | 封装认证、知识、会话、Agent、MCP 与流式错误 | 后端 API、独立 Go module | API 兼容矩阵；OpenAPI 覆盖漂移检查 | P1 |
| 19 | CLI：`cli/` | Agent-first JSON/NDJSON CLI、profile、MCP serve | Go SDK、keyring、后端 API | 保持 contract tests；明确服务版本兼容 | P1 |
| 20 | Python MCP Server：`mcp-server/` | 以 stdio/SSE/HTTP 暴露 WeKnora API | Python、`mcp`、API Key | 本次修复导入；后续修项目 URL、pytest 路径与 CLI MCP 定位 | P1 |
| 21 | 微信小程序：`miniprogram/`、`tests/miniprogram/` | 移动端配置、知识库选择、URL 导入与问答 | 微信开发者工具、后端 API | 私有配置隔离；网络 mock 和兼容声明 | P1 |
| 22 | Desktop/Lite：`cmd/{desktop,download}/`、`.env.lite.example` | SQLite 单体、桌面封装、Homebrew/systemd 分发 | CGO/SQLite、前端、可选 Wails | 标准版差异表；升级和数据迁移测试 | P1 |
| 23 | DocReader：`docreader/` | gRPC 多格式解析、图片/文本输出与健康检查 | Python/uv、解析器、Playwright、共享卷 | 修正文档描述；依赖 extras；格式/性能回归 | P1 |
| 24 | DB/Migration/Queue：`internal/database/`、`migrations/`、repository/异步 service | PostgreSQL/SQLite、迁移、Redis stream、异步任务与 DLQ | ParadeDB/PostgreSQL 或 SQLite、Redis | 真实升级矩阵；队列/DLQ/迁移指标 | P0/P1 |
| 25 | 对象存储：`internal/application/service/file/`、storage providers | 本地、MinIO 与多云对象存储 | 云凭证、Bucket、SSRF/域名策略 | provider 诊断；备份/生命周期/孤儿清理 | P1 |
| 26 | 可观测性：`internal/{tracing,logger}/`、`docs/Langfuse集成.md` | 日志、请求 ID、模型调试与 Langfuse trace | 可选 Langfuse 栈/Cloud | 示例 Key 默认留空；补 RED/队列/模型指标 | P0/P1 |
| 27 | Docker/脚本：`docker/`、`docker-compose*.yml`、`scripts/`、`Makefile` | 镜像、Compose profile、开发、迁移与打包 | Docker/Compose、可选 Go/Node | 安全化 env 检查；拉取恢复与代理提示 | P0 |
| 28 | Helm/Kubernetes：`helm/` | K8s、Ingress、PVC、Secret 和服务部署 | Kubernetes 1.25+、Helm 3.10+ | 版本单一来源；`helm lint/template` 门禁 | P1 |
| 29 | Prompt/内置资源：`config/`、`misc/`、`skills/`、`examples/`、`packages/` | Prompt、内置 Agent/模型、Dex/SearXNG、Skills 与示例 | 环境变量、运行时挂载 | YAML/Prompt/Skill schema；说明根 `packages` 占位 | P1/P2 |
| 30 | 文档/测试/样本：`docs/`、`testdata/`、`tests/`、`Comdoc/` | 用户/开发/API 文档、各语言测试与格式样本 | 各子工程测试框架 | 根级 `make verify`；文档命令可执行性 CI | P1 |

## 三、优先实施需求

### P0：安全与可运行性

1. 安全化 `.env.example`：Langfuse Key 默认空，所有默认秘密明确仅限本地并在生产启动时拒绝弱默认值。
2. 重写 `scripts/check-env.sh`：不执行 `.env` 内容、不输出秘密，仅校验 allowlist、类型、长度和 SET/UNSET。
3. 建立生产配置门禁：公网环境必须有 TLS、强随机密钥、备份、域名/CORS/SSRF allowlist 和管理员初始化策略。
4. 为 Auth/RBAC、IM webhook、Embed secure mode、Web Fetch、MCP approval 建立安全契约测试集。

### P1：质量、兼容与可维护性

1. 新增根级 `make verify`，串联：三个 Go module、前端 type-check/build/test、DocReader/MCP Python tests、小程序 tests、Compose config、Helm lint/template。
2. 建立版本单一来源，自动同步 VERSION、前端包版本、Compose/Helm 镜像、文档与 CLI/SDK 兼容矩阵。
3. 形成知识处理状态机：排队、解析、切分、后处理、索引、完成、停止、失败、重试与 DLQ 均有明确定义。
4. 将 `container.go`、`router.go` 按稳定业务域拆分，但保留组合根、路由策略自检和端到端启动测试。
5. 建立多实现契约测试：向量库、对象存储、数据源、IM、搜索、模型供应商共享同一套行为标准。
6. 为关键流式链路定义版本化事件协议，并覆盖客户端取消、断线、重复事件和部分失败。
7. 补齐恢复性：数据库/文件/队列一致备份，迁移前检查，升级/回滚演练与 DLQ 回放。
8. 在公开 auth config 中声明 `auto_setup_enabled`（或等价 capability），Standard 前端不再用预期 403 探测 Lite 能力。
9. 修正 Python MCP Server 的项目归属 URL 与 pytest 发现路径，并说明其与 CLI MCP serve 的定位。

### P2：体验与工程演进

1. 修正 `knowledage-base`、DocReader description、根 `packages/` 等低风险命名/说明问题。
2. 为管理端增加队列、DLQ、模型限流、连接器同步和解析阶段的诊断视图。
3. 为本地开发提供 Lite、Compose、源码 dev 三种模式的明确选择器与能力差异表。

## 四、P0 Issue 包与跨模块异常

| Issue | 范围 | 可验证验收 | 非范围 |
|---|---|---|---|
| P0-01 安全化示例配置 | `.env.example` 的 Langfuse/默认秘密说明与生产弱默认检查 | 复制示例后 Langfuse 不会因占位 Key 自动启用；秘密扫描无真实 Key；生产模式弱秘密检查返回明确失败 | 不代管真实供应商 Key，不部署 Langfuse |
| P0-02 安全化 env 检查 | `scripts/check-env.sh` 的解析、allowlist、脱敏输出 | 构造含 Shell 语句的测试 `.env`，脚本不执行；输出只含键名与 `SET/UNSET`，测试覆盖 DB/Redis/JWT | 不替代 Secret Manager，不修改业务运行配置 |
| P0-03 生产配置门禁 | TLS、强密钥、备份、域名/CORS/SSRF、管理员初始化检查 | 给定安全/不安全两套 fixture，门禁分别 exit 0/非 0，失败信息定位到配置项且不回显秘密 | 不实现集群高可用，不自动购买证书 |
| P0-04 安全契约回归 | Auth/RBAC、IM webhook、Embed、Web Fetch、MCP approval 的关键允许/拒绝路径 | 每类至少覆盖合法、越权/伪造、重放或 SSRF、审计事件；统一 CI 命令可重复通过 | 不接入真实第三方生产账号，不做大规模压测 |

P1-01～P1-09 对应第三节 P1 的 1～9 项，P2-01～P2-03 对应 P2 的 1～3 项；拆票时沿用相同顺序，并为每票补负责人、工期和目标版本。异常场景至少覆盖：模型未配置时核心服务可启动但 RAG 明确提示、DocReader 不可达时任务可重试/停止、队列积压可观测且可回放、第三方限流采用退避、批量操作部分成功可定位失败项。

## 五、推荐实施顺序

1. 先完成 P0 配置与安全门禁，保证“能启动”不以弱秘密或误连服务为代价。
2. 建立统一 verify 和版本单一来源，让后续重构有稳定回归网。
3. 收敛知识处理/流式事件/多适配器契约，再拆大型组合文件。
4. 最后增加诊断 UI、Lite/标准差异与其他 P2 体验项。

## 六、本文档验收标准

- 每个模块都有明确入口、用途、依赖与至少一个验证方式或优化方向。
- P0 已包含范围、可验证验收与非范围；P1/P2 有稳定编号映射，可在排期时补负责人、工期、目标版本后拆为独立 issue。
- 默认核心服务可在无第三方模型 Key 时健康启动；未配置模型产生可理解的业务提示而非 panic。
- 所有秘密不进入 Git，配置检查不回显秘密。
- 统一验证命令在干净环境可重复运行，失败项能定位到具体子工程。
- 首屏不以 4xx 请求探测 edition/capability，浏览器控制台不出现可避免的预期错误。
